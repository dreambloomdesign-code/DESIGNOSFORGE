# DesignMathEngine

Mathematical design layer for DESIGNOSFORGE v2.1.0.

Use for mixed-language vectorization, cosine similarity, jaccard similarity, softmax routing, entropy, confidence margin, Pareto front, TOPSIS ranking, weighted utility, and constraint penalty.

Validation:

- expose formulas through `math_trace`
- keep scores normalized where needed
- do not make black-box taste claims without route or critic evidence
